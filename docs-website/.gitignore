# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Demo PDFs and thumbnails, written by `pnpm run demos` from the SampleApp. Never committed: they
# are rebuilt from the code on every deploy, so they cannot drift from it.
/static/demos

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.pnpm-store
